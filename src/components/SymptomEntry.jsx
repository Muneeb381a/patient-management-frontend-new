import React from 'react';

const SymptomEntry = ({ symptoms, allSymptoms, addSymptom, loading, error }) => (
  <div className="bg-white p-6 rounded-lg shadow-md">
    {/* Symptom entry UI */}
  </div>
);

export default SymptomEntry;